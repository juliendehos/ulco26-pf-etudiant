module SortSpec (main, spec) where

import Data.Vector as V ( empty, fromList, Vector )
import Test.Hspec

import Sort

main :: IO ()
main = hspec spec

spec :: Spec
spec = do
    pure ()
    -- TODO


